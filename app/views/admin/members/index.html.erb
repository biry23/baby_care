<h1>ユーザー一覧</h1>
<table>
  <thead>
    <tr>
      <th>id</th>
      <th>名前</th>
      <th>メール</th>
      <th>投稿数</th>
      <th>登録日時</th>
    </tr>
  </thead>
  <tbody>
    <% @members.each do |member| %>
    <tr>
      <td><%= member.id %></td>
      <td><%= link_to member.name, admin_member_path(member.id) %></td>
      <td><%= member.email %></td>
      <td><%= current_member.posts.count %></td>
      <td><%= member.crated_at %></td>
    </tr>
    <% end %>
  </tbody>
</table>

