<h1>ユーザー情報</h1>
<%= link_to "編集", edit_member_path(@member.id) %>
<table>
  <tbody>
    <tr>
      <th>ニックネーム</th>
      <td><%= @member.member_name %></td>
    </tr>
    <tr>
      <th>メールアドレス</th>
      <td><%= @member.email %></td>
    </tr>
    <tr>
      <th>年齢</th>
      <td><%= @member.age %></td>
    </tr>
    <tr>
      <th>性別</th>
      <td><%= @member.gender %></td>
    </tr>
    <tr>
      <th>投稿数</th>
      <td><%= @member.posts.count %></td>
    </tr>
    <tr>
      <th>コメント投稿数</th>
      <td><%= @member.comments.count %></td>
    </tr>
  </tbody>
</table>
<h3>ここにお気に入り一覧遷移ボタン作成する</h3>
<%= link_to "投稿一覧", posts_path %>
<%= link_to "投稿する", new_post_path %>